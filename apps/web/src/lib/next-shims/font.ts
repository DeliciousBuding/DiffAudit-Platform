/**
 * next/font shim.
 *
 * Inter is bundled via @fontsource/inter (static weights imported from the
 * entry point). The variable slot matches what the root layout expects so
 * existing `inter.variable` usage keeps working.
 */

export const Inter = {
  variable: "--font-sans",
};
