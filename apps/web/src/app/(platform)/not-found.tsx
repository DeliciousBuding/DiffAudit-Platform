export default function NotFoundPage() {
  return (
    <div className="glass-card p-6">
      <h1 className="text-2xl font-semibold">页面不存在</h1>
      <p className="mt-3 text-sm text-[var(--muted)]">
        当前平台壳只搭了核心页面骨架，请从左侧导航切换。
      </p>
    </div>
  );
}
